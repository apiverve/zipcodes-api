plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.13'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'zipcodes-api'
            version = '1.1.13'

            from components.java

            pom {
                name = 'Zip Codes Lookup'
                description = 'Zip Codes Lookup is a simple tool for looking up the zip codes of a city. It returns the zip codes of the city.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/zipcodes-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/zipcodes-api.git'
                    url = 'https://github.com/apiverve/zipcodes-api'
                }
            }
        }
    }
}
